projectname = ${project.name}
source.path = ${project.path}

remote.user = rix
remote.host = bolt.d64
remote.privkey = /home/${remote.user}/.ssh/d64
remote.pubkey = /home/${remote.user}/.ssh/d64.pub

remote.home.path = /home/${remote.user}
remote.app.path = ${remote.home.path}/${projectname}
remote.extensions.path = ${remote.app.path}/extensions
remote.web.path = ${remote.app.path}/web
remote.theme.path = ${remote.web.path}/theme

source.extensions.path = ${source.path}/extensions
source.theme.path = ${source.path}/theme

sync.exclude.php = deploy-php.exc

verbose = true