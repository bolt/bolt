{% set sub = [] %}
{% for extension in app.extensions.getMenuoptions %}
    {% set sub = sub|merge([
        {
            icon: extension.icon|default('icon-expand'),
            label: extension.label,
            link: extension.path
        }
    ]) %}
{% endfor %}

{{ nav.submenu('briefcase', __('Extensions'), sub, (page_nav == 'Settings/Extensions')) }}
