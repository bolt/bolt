<table class="dashboardlisting">
    <thead>
        <tr>
            <th>{{ __('page.ckeditor-browse-server.files') }}</th>
            <th style="min-width: 102px;">{{ __('page.ckeditor-browse-server.size') }}</th>
            <th style="min-width: 60px;">&nbsp;</th>
        </tr>
    </thead>

    <tbody>
        {% for file in context.files %}
            {% if file.type == 'image' %}

                <tr>
                    <td>

                        <i class="fa fa-fw fa-file-image-o"></i>

                        {% if file.readable %}
                            <a class="filebrowserCallbackLink" href="{{ file.url }}">
                                <b>{{ file.path|excerpt(60)|shy }}</b>
                            </a>
                        {% else %}
                            <b>{{ file.path|excerpt(60)|shy }} </b><i class="fa fa-lock"></i>
                        {% endif %}
                    </td>
                    <td>
                        {{ file.sizeformatted }}<br>
                        {% if file.type == 'image' %}{{ file.info.width }} x {{ file.info.height }} px {{ __('page.ckeditor-browse-server.pixel') }}{% endif %}
                    </td>
                    <td>
                        {{ file.path|showimage(54, 40, 'c') }}
                    </td>
                    {# <td>
                        <a href="{{ file.path|thumbnail(1000, 1000, 'r') }}" class="btn btn-mini magnifig" title="Image: {{ file.filename }}">{{ __('Preview') }}</a>
                    </td> #}
                </tr>
            {% endif %}
        {% endfor %}
    </tbody>
</table>
