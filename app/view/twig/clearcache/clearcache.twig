{# Page: NavSecondary > Configuration > Clear the cache #}

{% extends '@bolt/_base/_page-nav.twig' %}

{% block page_nav 'Settings/Configuration' %}

{% block page_title __('general.phrase.clear-cache') %}

{% block page_main %}

    <div class="row">
        <div class="col-xs-12">
            <p>
                <a href="{{ path('clearcache') }}" class="btn btn-primary">{{ __('general.phrase.clear-cache-again') }}</a>
            </p>
        </div>
    </div>

{% endblock page_main %}
