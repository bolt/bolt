{# NavSecondary > Content ... > Edit > CkEditor > Link > Browse Server #}
<ul>
    {% for file in stack()[:context.count] %}
        <li>
            <a href="{{ file.url }}" class="filebrowserCallbackLink">
                {{- file.filename }}
                <small>
                    (
                    {{- file.sizeFormatted(true) }}
                    {%- if file.image %}, {{ file.width }} × {{ file.height }} px{% endif -%}
                    )
                </small>
            </a>
        </li>
    {% endfor %}
</ul>
