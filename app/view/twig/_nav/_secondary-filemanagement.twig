{% set files_menu = app['menu.admin'].child('files') %}
{% set sub = [
    {
        icon: files_menu.child('files_uploads').icon,
        label: files_menu.child('files_uploads').label,
        link: files_menu.child('files_uploads').uri,
        isallowed: files_menu.child('files_uploads').permission
    }, {
        icon: files_menu.child('files_themes').icon,
        label: files_menu.child('files_themes').label,
        link: files_menu.child('files_themes').uri,
        isallowed: files_menu.child('files_themes').permission
    }
] %}

{{ nav.submenu('fa:folder-open', __('File Management'), sub, (page_nav == 'Settings/FileManagement')) }}
