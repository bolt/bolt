{% include '_header_basic.twig' %}

{% if active is not defined %}{% set active="dashboard" %}{% endif %}

<div id="wrapper">

    <nav class="navbar navbar-static-top navbar-inverse navbar-bolt" role="navigation" style="margin-bottom: 0">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".sidebar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('dashboard') }}">
                {# <img src="{{ paths.app }}view/img/bolt-icon.svg"> #}
                Bolt
            </a> 
            <a href="{{ path('dashboard') }}" target="_blank" class="navbar-site hidden-xs"><span>{% if app.config.get('general/sitename') %}{{ app.config.get('general/sitename') }}{% else %}Site{% endif %}</span></a>      
        </div>
        <!-- /.navbar-header -->

        <ul class="nav navbar-top-links navbar-right">

            {# one omnisearch here, for "small and up", the other in the side-navbar #}
            <form class="navbar-form hidden-xs" role="search">
                <div class="form-group has-feedback">
                    <input type="text" class="form-control semitransparent omnisearch" placeholder="Omnisearch…">
                    <span class="form-control-feedback semitransparent"><i class="fa fa-search"></i></span>
                </div>
            </form>

            <li>
                <a href="{{ paths.root }}" target="_blank" class="hidden-xs">
                    <i class="fa fa-external-link-square"></i>
                    {{ __('View site')}} 
                </a>
            </li>

            <!-- /.dropdown -->
            <li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                    <i class="fa fa-user fa-fw"></i> <span>{{ user.displayname|trimtext(16) }}</span> <i class="fa fa-caret-down"></i>
                </a>
                <ul class="dropdown-menu dropdown-user" role="menu" >
                    <li>
                        <a href="{{ path('profile') }}" role="menuitem" tabindex="-1"><i class="fa fa-gear fa-fw"></i> {{ __('Profile') }}</a>
                    </li>
                    <li class="divider"></li>
                    <li>
                        <form action="{{ path('logout') }}" method="POST">
                            <button class="btn btn-link btn-block" type="submit">
                                <i class="fa fa-sign-out"></i> {{ __('Logout') }}
                            </button>
                        </form>
                    </li>
                </ul>
                <!-- /.dropdown-user -->
            </li>
            <!-- /.dropdown -->
        </ul>
        <!-- /.navbar-top-links -->

    </nav>
    <!-- /.navbar-static-top -->


    <nav class="navbar-default navbar-static-side" role="navigation" id="nav">
        {% include '_sub_mainmenu.twig' %}
    </nav>

    <div id="page-wrapper">

    {% include '_messages.twig' %}

