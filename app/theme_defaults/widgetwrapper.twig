{% spaceless %}
    <div class='widget {{ widget.class|join(' ') }}' id='widget-{{ widget.key }}' data-key='{{ widget.key }}'
    {%- if widget.defer %} data-defer='true'{% endif -%}
    >
        {{ html|default('<!-- placeholder for ' ~ widget.key ~ ' -->')|raw }}
    </div>
{% endspaceless %}